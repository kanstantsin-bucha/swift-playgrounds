import XCTest
@testable import SwiftGraphQLPackage

final class SwiftGraphQLPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftGraphQLPackage().text, "Hello, World!")
    }
}
