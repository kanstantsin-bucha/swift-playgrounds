import XCTest
@testable import GraphClientPackage

final class GraphClientPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GraphClientPackage().text, "Hello, World!")
    }
}
